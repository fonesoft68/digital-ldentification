#include "ocr.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int isNun_5(unsigned char *res) {
	int judge = 0;

	//实现部分，返回1表示识别出，0表示识别不出
        //把res上下颠倒，运行isNun_2判断
	return judge;
}
